# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This is a sandbox repository ("Claude Code Sandbox" per `README.md`). At present it contains no source code, build configuration, dependencies, or tooling — only this file and the README.

There are no build, lint, or test commands to document yet, and no architecture to summarize. When real code is added, expand this file with the project's commands and the cross-file context a new instance would need to be productive.
